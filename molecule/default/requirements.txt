# molecule/default/requirements.txt
# Python dependencies for running the molecule test suite.
# Install with: pip install -r molecule/default/requirements.txt

ansible-core>=2.14
ansible-lint>=6.0
molecule>=6.0
molecule-docker>=2.1
pytest-testinfra>=10.0
PyYAML>=6.0
