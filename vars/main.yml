---
# vars/main.yml
# Internal role constants — not part of the public interface.
# Do not override these in host_vars or group_vars.

# Version being installed — update when the role is used for a future release.
_sendy_upgrade_version: "7.0.2"
